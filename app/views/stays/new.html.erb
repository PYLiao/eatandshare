<div class="ohyeah-box col-xs-10 col-xs-offset-1">
<h1>Enter Your Place Details</h1>
<%= simple_form_for @stay do |f| %>
	<%= f.input :name %>
	<%= f.input :address %>
	<%= f.input :description %>
	<br />
	<%= f.submit 'Share', :class => 'btn btn-warning'%>
	<% end %>
</div>